function initShapes(game){
	game.shapes = {
		player1 : function(params){
			
			// Testing something
			// Yay, the reference is maintained!
			//log(game.keymap)
			
			/*polygon({
				pointsX : 
				pointsY :
				offsetX :
				offsetY :
				color :
				context : 
			})*/
		}
	};
}